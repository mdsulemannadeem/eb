<%- include('./partials/header') %>

<% if(success && success.length > 0){ %>
    <div class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-green-500">
        <span class="inline-block mt-1 mb-1 text-white">
            <%= success %>
        </span>
    </div>
<% } %>

<% if(typeof error !== 'undefined' && error && error.length > 0){ %>
    <div class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-red-500">
        <span class="inline-block mt-1 mb-1 text-white">
            <%= error %>
        </span>
    </div>
<% } %>

<div class="min-h-screen flex flex-col">
    <div class="container mx-auto px-4 sm:px-6 lg:px-10 py-6 sm:py-10 lg:py-16">
        <div class="flex flex-col md:flex-row gap-6">
            <!-- Sidebar - stacks vertically on mobile -->
            <div class="w-full md:w-1/4 lg:w-1/5">
                <div class="bg-gray-50 p-4 rounded-lg shadow-sm mb-6 md:mb-0">
                    <h3 class="font-semibold text-lg mb-3">Admin Menu</h3>
                    <div class="flex flex-row md:flex-col space-x-4 md:space-x-0 md:space-y-2">
                        <a class="block w-fit hover:text-blue-600 transition-colors" href="/owners/admin">All Products</a>
                        <a class="block w-fit font-bold text-blue-600" href="/products/create">Create new product</a>
                    </div>
                </div>
            </div>
            
            <!-- Main content area - expands on mobile -->
            <main class="w-full md:w-3/4 lg:w-4/5 bg-white p-4 sm:p-6 lg:p-8 rounded-lg shadow">
                <h2 class="text-xl sm:text-2xl font-bold mb-6">Create New Product</h2>
                
                <form action="/products/create" method="POST" enctype="multipart/form-data">
                    <!-- Product Images Section -->
                    <div class="mb-6">
                        <label for="product-images" class="block mb-2 font-medium">Product Images</label>
                        <p class="text-sm text-gray-500 mb-3">Upload up to 5 images. First image will be the main product image.</p>
                        <p class="text-xs text-blue-600 mb-3">📁 Maximum file size: 50MB per image. Supported formats: JPG, PNG, GIF, WebP</p>
                        
                        <div class="space-y-3">
                            <div class="relative">
                                <input
                                    id="product-images-1"
                                    name="image"
                                    type="file"
                                    class="w-full py-2 px-4 border border-gray-300 rounded focus:border-blue-500 focus:ring-1 focus:ring-blue-500"
                                    multiple
                                    required
                                >
                                <div class="text-xs text-gray-500 mt-1">Main product image (required)</div>
                            </div>
                            
                            <div class="relative">
                                <input
                                    id="product-images-2"
                                    name="image"
                                    type="file"
                                    class="w-full py-2 px-4 border border-gray-300 rounded focus:border-blue-500 focus:ring-1 focus:ring-blue-500"
                                    multiple
                                >
                                <div class="text-xs text-gray-500 mt-1">Additional image (optional)</div>
                            </div>
                            
                            <div class="relative">
                                <input
                                    id="product-images-3"
                                    name="image"
                                    type="file"
                                    class="w-full py-2 px-4 border border-gray-300 rounded focus:border-blue-500 focus:ring-1 focus:ring-blue-500"
                                    multiple
                                >
                                <div class="text-xs text-gray-500 mt-1">Additional image (optional)</div>
                            </div>
                            
                            <div class="relative">
                                <input
                                    id="product-images-4"
                                    name="image"
                                    type="file"
                                    class="w-full py-2 px-4 border border-gray-300 rounded focus:border-blue-500 focus:ring-1 focus:ring-blue-500"
                                    multiple
                                >
                                <div class="text-xs text-gray-500 mt-1">Additional image (optional)</div>
                            </div>
                            
                            <div class="relative">
                                <input
                                    id="product-images-5"
                                    name="image"
                                    type="file"
                                    class="w-full py-2 px-4 border border-gray-300 rounded focus:border-blue-500 focus:ring-1 focus:ring-blue-500"
                                    multiple
                                >
                                <div class="text-xs text-gray-500 mt-1">Additional image (optional)</div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Product Details Section -->
                    <div class="mb-6">
                        <h3 class="text-lg font-semibold mb-3">Product Details</h3>
                        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                            <div>
                                <label for="product-name" class="block text-sm font-medium mb-1">Product Name</label>
                                <input 
                                    id="product-name"
                                    name="name" 
                                    type="text" 
                                    placeholder="Product Name" 
                                    class="w-full border border-gray-300 p-2 rounded focus:border-blue-500 focus:ring-1 focus:ring-blue-500" 
                                    required
                                >
                            </div>
                            <div>
                                <label for="product-price" class="block text-sm font-medium mb-1">Price (₹)</label>
                                <input 
                                    id="product-price"
                                    name="price" 
                                    type="number" 
                                    placeholder="Product Price" 
                                    class="w-full border border-gray-300 p-2 rounded focus:border-blue-500 focus:ring-1 focus:ring-blue-500" 
                                    required
                                >
                            </div>
                            <div>
                                <label for="discount-price" class="block text-sm font-medium mb-1">Discount (₹)</label>
                                <input 
                                    id="discount-price"
                                    name="discount" 
                                    type="number" 
                                    placeholder="Discount Amount" 
                                    class="w-full border border-gray-300 p-2 rounded focus:border-blue-500 focus:ring-1 focus:ring-blue-500"
                                >
                            </div>
                            <!-- Add Tax Field -->
                            <div>
                                <label for="product-tax" class="block text-sm font-medium mb-1">Tax (%)</label>
                                <input 
                                    id="product-tax"
                                    name="tax" 
                                    type="number" 
                                    step="0.01"
                                    min="0"
                                    max="100"
                                    placeholder="Tax Percentage (e.g., 18 for 18%)" 
                                    class="w-full border border-gray-300 p-2 rounded focus:border-blue-500 focus:ring-1 focus:ring-blue-500"
                                    value="18"
                                >
                                <div class="text-xs text-gray-500 mt-1">GST/Tax percentage (default: 18%)</div>
                            </div>
                        </div>
                    </div>

                    <!-- Additional Details Section -->
                    <div class="mb-6">
                        <h3 class="text-lg font-semibold mb-3">Additional Details</h3>
                        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                            <div class="sm:col-span-2">
                                <label for="product-description" class="block text-sm font-medium mb-1">Description</label>
                                <textarea 
                                    id="product-description"
                                    name="description" 
                                    placeholder="Product Description" 
                                    rows="4"
                                    class="w-full border border-gray-300 p-2 rounded focus:border-blue-500 focus:ring-1 focus:ring-blue-500" 
                                    required
                                ></textarea>
                            </div>
                            <div>
                                <label for="product-category" class="block text-sm font-medium mb-1">Category</label>
                                <input 
                                    id="product-category"
                                    name="category" 
                                    type="text" 
                                    placeholder="Product Category" 
                                    class="w-full border border-gray-300 p-2 rounded focus:border-blue-500 focus:ring-1 focus:ring-blue-500"
                                >
                            </div>
                        </div>
                    </div>

                    <!-- Product Type Section -->
                    <div class="mb-6">
                        <h3 class="text-lg font-semibold mb-3">Product Type</h3>
                        <div>
                            <label for="product-type" class="block text-sm font-medium mb-1">Select Type</label>
                            <select 
                                id="product-type"
                                name="productType" 
                                class="w-full border border-gray-300 p-2 rounded focus:border-blue-500 focus:ring-1 focus:ring-blue-500" 
                                required
                            >
                                <option value="">Select Product Type</option>
                                <option value="Beginner Banjo">Beginner Banjo</option>
                                <option value="Professional Banjo">Professional Banjo</option>
                                <option value="Accessories">Accessories</option>
                            </select>
                        </div>
                    </div>

                    <!-- Panel Details Section -->
                    <div class="mb-8">
                        <h3 class="text-lg font-semibold mb-3">Display Colors</h3>
                        <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
                            <div>
                                <label for="bg-color" class="block text-sm font-medium mb-1">Background Color</label>
                                <input 
                                    id="bg-color"
                                    name="bgcolor" 
                                    type="text" 
                                    placeholder="Background Color (e.g. #ffffff)" 
                                    class="w-full border border-gray-300 p-2 rounded focus:border-blue-500 focus:ring-1 focus:ring-blue-500" 
                                >
                            </div>
                            <div>
                                <label for="panel-color" class="block text-sm font-medium mb-1">Panel Color</label>
                                <input 
                                    id="panel-color"
                                    name="panelcolor" 
                                    type="text" 
                                    placeholder="Panel Color (e.g. #000000)" 
                                    class="w-full border border-gray-300 p-2 rounded focus:border-blue-500 focus:ring-1 focus:ring-blue-500" 
                                >
                            </div>
                            <div>
                                <label for="text-color" class="block text-sm font-medium mb-1">Text Color</label>
                                <input 
                                    id="text-color"
                                    name="textcolor" 
                                    type="text" 
                                    placeholder="Text Color (e.g. #000000)" 
                                    class="w-full border border-gray-300 p-2 rounded focus:border-blue-500 focus:ring-1 focus:ring-blue-500" 
                                >
                            </div>
                        </div>
                    </div>

                    <button type="submit" class="w-full sm:w-auto bg-blue-600 hover:bg-blue-700 text-white font-medium px-6 py-3 rounded transition-colors">Create Product</button>
                </form>
            </main>
        </div>
    </div>
</div>

<script>
// File size validation on client side
document.addEventListener('DOMContentLoaded', function() {
    const fileInputs = document.querySelectorAll('input[type="file"]');
    const maxFileSize = 50 * 1024 * 1024; // 50MB in bytes
    
    fileInputs.forEach(input => {
        input.addEventListener('change', function(e) {
            const files = e.target.files;
            let totalSize = 0;
            let hasError = false;
            
            for (let i = 0; i < files.length; i++) {
                const file = files[i];
                totalSize += file.size;
                
                // Check individual file size
                if (file.size > maxFileSize) {
                    alert(`File "${file.name}" is too large. Maximum size is 50MB per file.`);
                    e.target.value = ''; // Clear the input
                    hasError = true;
                    break;
                }
                
                // Check file type
                if (!file.type.startsWith('image/')) {
                    alert(`File "${file.name}" is not an image. Please select only image files.`);
                    e.target.value = ''; // Clear the input
                    hasError = true;
                    break;
                }
            }
            
            // Show file size info
            if (!hasError && files.length > 0) {
                const sizeInfo = input.parentElement.querySelector('.file-size-info');
                if (sizeInfo) {
                    sizeInfo.remove();
                }
                
                const totalSizeMB = (totalSize / (1024 * 1024)).toFixed(2);
                const info = document.createElement('div');
                info.className = 'file-size-info text-xs text-gray-600 mt-1';
                info.textContent = `Selected: ${files.length} file(s), Total size: ${totalSizeMB} MB`;
                input.parentElement.appendChild(info);
            }
        });
    });
});
</script>

<%- include('./partials/footer') %>